language: node_js
node_js:
  - "8"
script:
  - yarn
  - npm run build
cache: yarn
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: false
  local-dir: dist
  on:
    branch: gh-pages